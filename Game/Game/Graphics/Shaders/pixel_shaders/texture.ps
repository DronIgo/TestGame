#version 450 core 

struct Material {
	sampler2D texture_diffuse1;
}; 
uniform Material material;

in vec3 Normal;
in vec2 Tex;

out vec4 fragColor;

void main() {
	fragColor = texture(material.texture_diffuse1, Tex);//vec4(1.0, 0.0, 0.0, 1.0);//
}